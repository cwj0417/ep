name: update
on: workflow_dispatch
permissions:
  contents: write
jobs: 
  recievedata:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v4
        - uses: actions/setup-node@v4
          with:
            node-version: "20.x"
        - run: node scripts/updateDataFromAction.js