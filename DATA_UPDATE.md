# data.json 更新指南

症状日记 → `src/data.json` 的录入规范(供无上下文时参考)。

## 数据结构

顶层对象,键为日期字符串 `"YYYY-MM-DD"`(升序),值为当天记录:

```json
"2026-07-17": {
    "detail": "没麻\n今天看病，奥卡西平要自费了\n12:55不舒服，...",
    "coner": ["医院", "看病, 奥卡西平要自费了"],
    "hh": [0, 1, 2, 12, 17],
    "s3": 0,
    "s4": 2
}
```

## 字段说明

| 字段 | 类型 | 含义 |
|------|------|------|
| detail | string | 原文记录(必须)。首行通常是"麻"的状态(没麻/不麻/忘记有没有麻等),之后每行按时间线一条 |
| coner | [标签, 描述] | 药物调整/就医/重大事件,无则整个字段省略。标签示例:`布1`(布利西坦开始)、`奥减`(奥卡西平减量)、`左500`、`医院`、`妇婴` |
| hh | number[] | 当天提到的所有时间点的小时数,去重升序(如 `12:55`→12、`00.35`→0) |
| s3 | number | 重度发作次数(前端标签"小发"):单次评分 ≥20 分,或鬼压床,或明确说"小发" |
| s4 | number | 轻微发作次数(前端标签"轻微"):评分 <20 的所有发作,含无评分的"梦境/熟悉/不舒服过/发做过" |

## 统计口径(从历史数据归纳)

- 发作分值体系为自评,常见 2~30 分
- **计入 s3/s4**:带分值的发作、梦境、熟悉感、"发做过/不舒服过"、鬼压床、明确"小发"
- **不计入**:单纯打嗝、太阳穴紧/疼、心跳快、无分值的"手麻"、情绪事件(吵架/哭)、回答"没有/不知道/忘记"
- 模糊计数处理:
  - "有过 2,3 次"取上限 3(参考 2026-07-14 `s4:3`)
  - "5 到 10 次左右"取下限 5(参考 2026-07-02 `s4:7` = 2+5)
  - "可能有过一次 10"计入(参考 2026-07-03 `s4:2`)
- hh 记录所有提及的时间点(含非发作事件如吵架、吃饭),只取小时并去重
- 评分 10~20 之间(如 12、15 分)归 s4;"没有过 20"的表述说明当天无 s3 级发作

## 更新流程

1. 把日记按日期分块(如 `7.17`),原文去掉日期行放入 `detail`,行间用 `\n`,保留原有标点
2. 按口径统计 `s3`/`s4`,提取 `hh`,识别 `coner` 事件(开始/减/停药、就医、重大事件)
3. **文本追加方式**在文件末尾 `}` 前插入新记录块(保持旧内容零改动;文件末尾无换行符)
4. 校验 JSON:`python3 -c "import json; json.load(open('src/data.json'))"`
5. `git diff` 确认纯新增后再提交

## 历史提交风格

`chore: update data`
